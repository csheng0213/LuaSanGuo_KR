--@auto code head
local BaseView = require("app.baseMVC.BaseView")
local GuildBattleView = class("GuildBattleView", BaseView)

local Functions = require("app.common.Functions")

GuildBattleView.csbResPath = "lk/csb"
GuildBattleView.debug = true
--@auto code head end


--@auto code uiInit
function GuildBattleView:onInitUI()

    --output list
    
    --label list
    
    --button list
    
end
--@auto code uiInit end


return GuildBattleView