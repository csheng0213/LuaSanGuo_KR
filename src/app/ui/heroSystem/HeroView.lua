--@auto code head
local BaseView = require("app.baseMVC.BaseView")
local HeroView = class("HeroView", BaseView)

local Functions = require("app.common.Functions")

HeroView.csbResPath = "lk/csb"
HeroView.debug = true
--@auto code head end


--@auto code uiInit
function HeroView:onInitUI()

    --output list
    
    --label list
    
    --button list
    
end
--@auto code uiInit end


return HeroView