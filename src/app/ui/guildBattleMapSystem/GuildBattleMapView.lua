--@auto code head
local BaseView = require("app.baseMVC.BaseView")
local GuildBattleMapView = class("GuildBattleMapView", BaseView)

local Functions = require("app.common.Functions")

GuildBattleMapView.csbResPath = "lk/csb"
GuildBattleMapView.debug = true
--@auto code head end


--@auto code uiInit
function GuildBattleMapView:onInitUI()

    --output list
    
    --label list
    
    --button list
    
end
--@auto code uiInit end


return GuildBattleMapView